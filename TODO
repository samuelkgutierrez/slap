make sure that start and ends are balanced in inputs
fix regex at end alpha at begin
fix \n inputs. like:
alphabet
'b 'a 'o 'r 'i 'n 'g ' '\n
end
in input parse, need to add \n and maybe others like \t?
fix ' and '_
